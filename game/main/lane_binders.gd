extends Node2D


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	
	#var all_lanebound_entities = owner.get_children().filter(func(child_node) -> bool:
			#return child_node.has_member("lane_id"))
#
	#for laneitem in all_lanebound_entities:
		#var lane_entity := laneitem as Node2D
		#print(laneitem.name)
		
	return
